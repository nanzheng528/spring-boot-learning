my:
 name: nanzheng
 age: 24
 number: ${random.int}
 uuid: ${random.uuid}
 max: ${random.int(10)}
 value: ${random.value}
 greeting: hi,i'm ${my.name}

spring:
 profiles:
  active: dev
 activemq:
   broker-url: tcp://127.0.0.1:61616
   in-memory: true
   pool:
     enabled: false
